#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array

int main() {
   ios::sync_with_stdio(0);
   cin.tie(0);
   int m;
   cin >> m;
   while (m--){
    int n;
    cin >> n;
    vector<int> fila(2*n);
    for (int i = 0 ; i < 2*n ; ++i){
        cin >> fila[i];
    }
   }
   return 0;
}